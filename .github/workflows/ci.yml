name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    name: Swift Tests
    runs-on: macos-15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Run tests
        run: swift test
